# Website Audit Tool With Lighthouse
A Node.js utility to audit and generate insights for websites.

## Installation

1. Install the required dependencies:

```bash
npm install
```

## Usage

Run the audit script by passing the target URL as an argument.

```bash
node audit.js "<url>"
```

### Example
To audit example.com:

```
node audit.js "https://example.com"
```